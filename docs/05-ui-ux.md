# 05. UI / UX 設計（スマホ縦持ち）

## 5.1 前提

- 基準端末：**iPhone 幅 390px × 高さ 844px**（CSS px）。
- 高さは `100dvh` を使う（iOS Safari のアドレスバー伸縮対策）。`100vh` は使わない。
- `env(safe-area-inset-bottom)` を下部パディングに必ず加算する。
- **親指到達範囲は画面下 1/3**。主要操作（スピン／3択の決定）はここに置く。
- 上部は情報表示のみ。タップ対象を置かない。

## 5.2 メイン画面（ラン中）

```
┌──────────────────────────────┐  ← safe-area-top
│  第 3 期        ○○○●●        │  残スピン数（●=残り）
│  家賃 100                    │
│  💰 342                      │  所持コイン（大きく・等幅）
├──────────────────────────────┤
│                              │
│   ┌────┬────┬────┬────┐      │
│   │ 🐈 │ 🐁 │    │ 🪙 │      │
│   ├────┼────┼────┼────┤      │
│   │ 🍙 │ 🎒 │ 💡 │    │      │  4列 × 5行
│   ├────┼────┼────┼────┤      │  1マス 82×82px
│   │    │ 📻 │ 👵 │ 🌱 │      │  盤面全体 350×430px
│   ├────┼────┼────┼────┤      │
│   │ 🧦 │    │ 🧦 │ 💧 │      │
│   ├────┼────┼────┼────┤      │
│   │ 🪣 │ 🚲 │    │ 🍵 │      │
│   └────┴────┴────┴────┘      │
│                              │
│         + 47                 │  今回の獲得（盤面直下に大きく）
├──────────────────────────────┤
│      ╭──────────────╮        │
│      │    スピン     │        │  高さ 64px・幅 240px・画面中央
│      ╰──────────────╯        │
│   🎒 持ち物      📖 図鑑      │
└──────────────────────────────┘  ← safe-area-bottom
```

**縦の割り当て（844px の場合）**

| 領域 | 高さ |
| --- | --- |
| セーフエリア上 | 47 |
| ヘッダ（期・家賃・コイン） | 96 |
| 盤面 | 430 |
| 獲得表示 | 56 |
| 操作エリア（スピン＋サブ） | 180 |
| セーフエリア下 | 34 |
| 合計 | 843 |

小さい端末（高さ 667px）では**盤面のマスを縮める**（82 → 64px）。
盤面はアスペクト比 4:5 を保ったまま `min(90vw, 可用高さ*0.8)` で決める。

## 5.3 3択（ボトムシート）

スピン解決後、下から**ボトムシート**がせり上がる。盤面は上半分に残す
（**「今の盤面を見ながら選ぶ」ことがこのゲームの意思決定そのもの**なので、盤面を隠さない）。

```
│   （盤面は上に残る・やや暗く）    │
├──────────────────────────────┤
│  どれを入居させる？             │
│ ┌────────┬────────┬────────┐ │
│ │   🐔   │   🍺   │   🏆   │ │
│ │ ニワトリ│ 缶ビール│優勝カップ│ │
│ │ 3スピン │ 隣の住人│ 隣接の値 │ │
│ │ ごとに🥚│ に +3  │ を ×2   │ │
│ │ ★★☆☆ │ ★★☆☆ │ ★★★☆ │ │
│ └────────┴────────┴────────┘ │
│                              │
│      見送る（+5コイン）        │
└──────────────────────────────┘
```

- カードは**タップで選択確定ではなく、1タップで拡大表示 → もう1タップで確定**。
  誤タップでビルドが壊れる事故を防ぐ（このジャンルでは1枚の誤選択が致命傷になる）。
- 拡大表示中は、**そのシンボルと噛み合う所持シンボルを盤面上でハイライト**する
  （虫めがねアイテム所持時は常時）。初心者がシナジーを発見する主導線。

## 5.4 スピン演出

**「どのマスがいくら入れたか」を1マスずつ順に見せる**のがこの演出の主目的。
まとめて出すと数字が一度に増えるだけで、シナジーを組んだ実感が残らない。

順番は**盤面インデックスの昇順＝左上から右下への読み順**。
これはエンジンの効果解決順（02.3）と一致しており、説明が一本化される。

| t（盤面20マスが全部埋まった場合） | 内容 |
| --- | --- |
| 0.00s | スピンボタンが沈む。触覚フィードバック（`navigator.vibrate(8)`）。レバー音 |
| 0.00–0.26s | 各列が上から下へ順にスライドイン（列ごとに 34ms ずらし）。列ごとに停止音 |
| 0.26–0.43s | **破壊されたシンボルを先にグレーアウト**。「消えたから入らなかった」を先に見せる |
| 0.43–1.05s | **1マスずつ加算**。獲得値が**枠付きのラベル**としてタイルの上端から飛び出し、<br>盤面下の合計が積み上がる。1マスごとに音程の上がる加算音が鳴る |
| （倍率がある場合） | `+53 × 3` を見せてから最終値へ。専用の音 |
| 1.05–1.30s | コイン表示がカウントアップ |

- 1マスあたりの間隔は **620ms ÷ マス数**（下限28ms・上限95ms）。
  マスが増えても総尺が伸びないようにする。
- **画面のどこかをタップすると演出を即スキップ**（残りは一括表示）。
- **高速モード**：ボタンでON。演出を全部飛ばして即結果。長時間プレイ向け。
- `prefers-reduced-motion: reduce` の場合はスライドとポップを廃し、フェードのみ。
- 枠の強調は**いま加算中の1マスだけ**に付ける。加算済み全部に付けると、
  終盤には盤面のほぼ全マスが光って何の情報にもならない。加算済みかどうかは残る数字が伝える。

### 獲得値ラベルの置き方

**タイルの上端をまたぐ位置に、枠付きの金色のラベルとして出す。**

マスの隅に小さな数字を置くと「いくら入ったのか」が読み取れず、
逐次加算にした意味がなくなる。ラベルは盤面や隣のマスにはみ出してよい
（むしろ重なるほど派手で、スロットとしては正しい）。

- 位置: マス上端中央（`translate(-50%, -50%)`）。最上段は盤面の外にはみ出す
- 動き: 下から跳ね上がって少しオーバーシュートしてから落ち着く（0.3秒）
- 重なり: **いま加算中のラベルを最前面**に。それ以外は盤面の並び順どおり

> 当初は「合計1.2秒を上限」としていたが、逐次加算にしたことで
> 全マス埋まった状態では **約1.35秒**になった。テンポの実機確認は未実施（6.3の検証項目）。

## 5.4.1 効果音

音声ファイルは持たず、**Web Audio でその場で合成**する（`src/ui/audio.js`）。
アセット0・オフライン可・初回ロード増加なしを保ちつつ、
「加算のたびに音程を上げる」といった動的な鳴らし方が素直に書ける。

| 音 | 用途 |
| --- | --- |
| レバー / 列停止 | スピン開始と各列の着地 |
| 加算音 | 1マスごと。**ペンタトニックで音程が上がっていく**（積み上がりの快感の中心） |
| 破壊音 | シンボルが壊れたとき（ノイズバースト） |
| 倍率音 | 合計に倍率が乗ったとき |
| 支払い成功 / 失敗 / 完済 | 期の区切り |
| タップ / 購入 / 廃棄 | UI操作 |

- **ミュートトグルを常時表示**（下部ボタン）。設定は localStorage に保存し、リロードをまたぐ。
- ブラウザの自動再生制限があるため、**AudioContext は最初のタップまで生成しない**。
- 音は完全に任意。**無音で情報が欠落してはいけない**（5.8）。

## 5.5 情報設計の原則

1. **数字は常に見える。** コイン・家賃・残スピンは常時表示。モーダルで隠さない。
2. **「あと何コイン足りないか」を出す。** 家賃バーは残額を赤字で表示（`あと 58`）。
   これがラン中の緊張の源泉。
3. **効果テキストは盤面のマスを長押しで表示。** タップは選択、長押しは説明、で統一。
4. **色に意味を持たせない。** 色覚特性への配慮として、レアリティは★の数と枠線の形で示す。

## 5.6 リザルト画面

```
│      家賃、払えませんでした      │
│         第 8 期 で終了          │
│                                │
│  獲得コイン推移                  │
│    ▁▂▃▄▅▆▇█▇▅                 │  期別グラフ
│                                │
│  最終デッキ（42個）              │
│  🐈×3 🐁×6 🍙×8 ...            │
│                                │
│  死因: 第8期の平均が 41/spin     │  ← 必要値との差分を明示
│        （必要 68/spin）          │
│                                │
│  ╭────────────╮ ╭────────────╮ │
│  │ 同じシードで │ │  新しいラン │ │
│  ╰────────────╯ ╰────────────╯ │
```

**死因の可視化は必須機能**とする。「なぜ負けたか」がわからないゲームは2周目がない。

## 5.7 チュートリアル

チュートリアル専用ステージは作らない。**初回ランに埋め込む**。

| タイミング | 内容 |
| --- | --- |
| 初回スピン前 | 「レバーを引くとコインが出る」だけ。1文 |
| 初回3択 | 「絵柄を増やすと、スロットに並ぶ絵柄が増える」 |
| 隣接シナジー初発火 | 該当2マスを線で結んで「隣どうしで効果が出た」 |
| 初回家賃 | 「4スピンごとに家賃。払えないと終わり」 |
| 初回ショップ | 「絵柄は捨てられる。減らすほど、残りが出やすくなる」← 最重要概念 |

一度に1つ、**吹き出し1文＋該当箇所のハイライト**のみ。全文スキップ可。

## 5.8 アクセシビリティ

- タップ対象は **44×44px 以上**。
- 盤面セルに `aria-label="ノラ猫 2コイン"` を付与。
- `prefers-reduced-motion` 対応（5.4）。
- 音は完全に任意。**無音で情報が欠落しないこと**（SEは演出の補強のみ）。
- 文字サイズは `rem` ベース。端末の文字拡大設定に追従（盤面は絵柄なので影響なし）。

## 5.9 アートディレクション

- **絵柄は絵文字ベースで開始**（開発速度優先、40種を即日用意できる）。
  ただし 04.9 の通り、後から SVG へ差し替えられる抽象を挟む。
- 背景：くすんだ生成り色（畳・砂壁のイメージ）。盤面枠は木製の質感。
- フォント：日本語は端末標準（`system-ui`）。数字だけ等幅にして桁揺れを防ぐ。
- ダークモード対応（`prefers-color-scheme`）。夜に布団の中で遊ぶ想定。
